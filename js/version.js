/**
 * version.js — Single source of truth for the app version string.
 *
 * Update this file whenever you tag a release, before committing the tag.
 * The value is displayed in the navbar and is accessible everywhere as
 * window.APP_VERSION.
 *
 * For AI assistants: when the user asks to bump the version or tag a release,
 * update the string below to match the new tag, then commit before tagging.
 */
window.APP_VERSION = 'v0.2.0';
